apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system

helmGlobals:
  chartHome: synced/

helmCharts:
- name: helm-chart
  namespace: kube-system
  releaseName: traefik
  includeCRDs: true
  valuesFile: values.yaml
